//how strings work in java script


function palindrome(str) {
  // Good luck!
  return true;
}

start of a fuction is the name and what is pointed at E.G string
function palindrome(string) {
//then allways start with var, followed by the .functions. this is broken into three, .tolower case to make everything lowercase
//then .match to check for any of the same values in the string
// then .join to remove all spaces

//notice how any .funtions can be placed one after the other. all you need to know is what you need to todo and find the .function
 function palindrome(string) {
        var str = string.replace(/[^A-Z0-9]/ig, "").toLowerCase(),
        reverse = str.split('').reverse().join('');
    return str == reverse;
};
palindrome("eye");
